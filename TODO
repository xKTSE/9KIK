Update version 1.0
	- Handle android backbutton
	- Handle offline mode (via Store.js)

NTS:
	Zerver set up
	- zerver -drl web --port=8889

	Git
	- git add/commit/push origin master

	Heroku
	- git push heroku master

	- remove cache.manifest before any additional development***